FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies with legacy peer deps for React 19 compatibility
RUN npm install --legacy-peer-deps

# Copy source code (including scripts folder)
COPY . .

# Expose the correct port
EXPOSE 5173

# Start development server
CMD ["npm", "run", "dev"]